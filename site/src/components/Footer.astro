---
/**
 * Site-specific Footer wrapper.
 * Federation has no local About/Contact/Legal pages — all links are
 * external to aegis-initiative.com, so the default (localLinks=false)
 * is correct.
 */
import SharedFooter from '@aegis-initiative/design-system/components/Footer.astro';
---

<SharedFooter />
